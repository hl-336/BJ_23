1、自动化测试类型有哪些？能解决一些什么样的问题？
UI自动化测试、接口自动化测试、单元测试
解决问题：回归测试、压力测试、兼容性测试、提高测试效率，提升产品质量


2、某电商平台web系统基础版本耗时3个月已上线，现已进入产品版本迭代过程
中，平均每周会发布一些新的功能以及少量历史优化类容。在不考虑当前人力资源
的情况下，该项目是否适合做web自动化测试并简单说明分析过程？
不适合做web自动化测试，项目的周期比较短，新功能的需求变动比较频繁

3、元素定位练习题：
1 #注册流程
2 #1)打开TPSHOP商城进入注册页面，暂停3s；
3 #2)完成注册流程；
4 #3)元素定位方式不限；
5 element.click()#点击事件

import time
from selenium import webdriver
driver = webdriver.Chrome()
driver.get("http://tpshop-test.itheima.net/Home/user/reg.html")
driver.find_element_by_id("username").send_keys("15811111789")
driver.find_element_by_name("verify_code").send_keys("8888")
driver.find_element_by_name("password").send_keys("abc123456")
driver.find_element_by_name("password2").send_keys("abc123456")
driver.find_element_by_name("invite").send_keys("13800011002")
driver.find_element_by_class_name("regbtn").click()
time.sleep(3)
driver.quit()

#登录练习：
2 #1)打开TPSHOP商城进入登录界面
3 #2)使用注册练习注册成功的用户信息登录TPSHOP
import time

from selenium import webdriver
driver = webdriver.Chrome()
driver.get("http://tpshop-test.itheima.net/Home/user/login.html")
driver.find_element_by_id("username").send_keys("15811111181")
driver.find_element_by_name("password").send_keys("abc123456")
driver.find_element_by_name("verify_code").send_keys("8888")
driver.find_element_by_name("sbtbutton").click()
time.sleep(3)
driver.quit()
